# Supabase (Phase 3 — Auth + Cloud-Save). Kopiere nach .env.local und fülle aus.
# Der Publishable/anon-Key ist öffentlich (gehört ins Frontend) — RLS schützt die Daten.
# Den sb_secret_-Key NIEMALS hier oder im Frontend verwenden.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
