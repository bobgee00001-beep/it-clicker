{
  "name": "devops-clicker",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check": "node scripts/check-html.mjs",
    "test:e2e": "playwright test",
    "deploy:preview": "vercel deploy --yes --token=$VERCEL_TOKEN",
    "deploy:prod": "vercel deploy --prod --yes --token=$VERCEL_TOKEN"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "vite": "^7.2.7"
  }
}
